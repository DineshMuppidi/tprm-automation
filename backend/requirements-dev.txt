# Test, coverage, and security-scanning tooling — not shipped in the
# runtime image (backend/Dockerfile installs requirements.txt only).
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.27.2

pip-audit==2.10.1
detect-secrets==1.5.0
