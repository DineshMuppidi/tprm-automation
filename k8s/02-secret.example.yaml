# Template only — DO NOT commit a real filled-in copy of this file.
# Real secrets belong in a proper secrets manager (AWS Secrets Manager +
# the External Secrets Operator, or `kubectl create secret` from a value
# never written to disk/git) per the Phase 0 threat model and the
# security-hardening doc. This file exists so the required *keys* are
# documented in one place, not so anyone fills in real values here.
apiVersion: v1
kind: Secret
metadata:
  name: tprm-secrets
  namespace: tprm
type: Opaque
stringData:
  DATABASE_URL: "postgresql://tprm:REPLACE_ME@rds-endpoint:5432/tprm"
  AUTH_SECRET: "REPLACE_ME_openssl_rand_hex_32"
  ADMIN_API_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: ""
  HIBP_API_KEY: ""
  NVD_API_KEY: ""
  NEWSAPI_API_KEY: ""
